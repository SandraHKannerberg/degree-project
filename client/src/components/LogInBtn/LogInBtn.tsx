import { Button } from "react-bootstrap";

function LogInBtn() {
  return (
    <Button
      size="lg"
      style={{
        backgroundColor: "#85586f",
        border: "none",
        borderRadius: 0,
        color: "#EFE1D1",
      }}
    >
      Log In
    </Button>
  );
}

export default LogInBtn;
